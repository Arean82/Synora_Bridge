# Environments
venv/
backend/.venv/
.env

# Local runtime configuration — contains REAL secrets (secret_key,
# encryption_key, DB passwords). Never commit. Template: backend/.env.example.
backend/config.ini
backend/config.ini.bak

# Launcher runtime state (frontend_port, theme prefs) — regenerated on use.
launcher/launcher.json

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Databases (any location)
*.db
*.sqlite3
bridge.db
instance/

# Logs
*.log
logs/

# All-in-one build staging (clean backend copy + node runtime for bundling)
installer_stage/

# Django
backend/staticfiles/
backend/db.sqlite3
backend/data/

# PyInstaller / Build artifacts
build/
dist/
*.exe

# IDE / Editor
.vscode/
.idea/
.ruby-lsp/

# Project Specific
scratch/
sample/
logs/

# Node / JS
node_modules/
sample2
frontend/.output/
frontend/.nuxt/
frontend/.data/

# Deepwork state (git-local)
.slim/deepwork/

# Memurai / Redis runtime artifacts
dump.rdb
memurai-log.txt


